Binary image placeholders were intentionally removed from this repository because the
PR creation flow used in this environment does not support binary files.

If you want branded assets locally, add the following files before building release
artifacts and update `app.json` again:

- `icon.png`
- `splash.png`
- `adaptive-icon.png`
